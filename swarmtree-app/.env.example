# Public Swarm gateway for reads (PRD §7.2)
VITE_BEE_GATEWAY_URL=https://api.gateway.ethswarm.org

# Programmatic upload backend. Default is "/api/swarm" — a relative path that
# Vite's dev server proxies to https://beeport.ethswarm.org/bzz server-to-server
# (see vite.config.ts → server.proxy). Beeport's CORS only allows their own
# origins, so direct browser→beeport calls fail; the proxy bypasses that.
# For a deployed app, point this at a proxy you control (Vercel function, etc.).
VITE_BEEPORT_BACKEND_URL=/api/swarm

# WalletConnect Cloud project id — public-by-design
# Get one at https://cloud.walletconnect.com
VITE_WALLETCONNECT_PROJECT_ID=

# Alchemy API key — one key works for both mainnet (ENS resolution) and
# Gnosis Chain (postage stamp registry reads). Get one at
# https://dashboard.alchemy.com (free tier is plenty).
# Note: Vite bakes this into the client bundle, so the key is public.
# Lock it down via Alchemy's Allowed Origins (Apps → Networks).
# If empty, falls through to public RPCs (LlamaRPC, Blast, gnosischain.com, gateway.fm).
VITE_ALCHEMY_API_KEY=
